# Dockge / Docker Compose settings
GRAV_PORT=8080

# Set true only when the mounted site directory needs its ownership repaired.
# The official Grav image already fixes permissions during first installation.
FIX_PERMISSIONS=false
